[project]
name = "ssh_honeypot"
version = "0.1.0"
dependencies = ["paramiko == 3.4", "requests == 2.32.5"]

[build-system]
requires = ["setuptools == 71.1.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
ssh-honeypot = "ssh_honeypot.app:main"
